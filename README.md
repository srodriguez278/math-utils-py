# Math Utils (Python)

This project implements a simple MathUtils class in Python and uses pytest
for unit testing. Jenkins is used to automatically run tests on each commit.

## Run tests locally

```bash
pip install -r requirements.txt
pytest
